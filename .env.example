# ============================================
# ContextOS — environment variables
# ============================================
# Copie pra .env.local e preencha.
# Nunca commitar .env.local

# --- Database (dev local via docker-compose) ---
# Porta 5433 pra evitar conflito com outros projetos rodando Postgres local na 5432
DATABASE_URL=postgres://contextos:contextos_dev@localhost:5436/contextos

# --- Redis (cache + queue) ---
# Porta 6381 pra evitar conflito com outros projetos rodando Redis local
REDIS_URL=redis://localhost:6381

# --- Auth ---
NEXTAUTH_SECRET=change_me_jwt_secret_at_least_32_chars_long
NEXTAUTH_URL=http://localhost:3000

# --- LLM Providers (pelo menos 1 obrigatório) ---
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# --- Embeddings (default: openai com text-embedding-3-small) ---
EMBEDDING_PROVIDER=openai
EMBEDDING_MODEL=text-embedding-3-small

# --- Storage (filesystem local no MVP) ---
STORAGE_DIR=./data/storage

# --- App ---
NODE_ENV=development
LOG_LEVEL=info
